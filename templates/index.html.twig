{% extends 'base.html.twig' %}

{% block title %}{{ 'default.index.titre' | trans }}{% endblock %}

{% block body %}
    <div class="card col-md-12">
        <div class="card-header">
            <h1>
                <i class="fas fa-home">{{ 'default.index.header' | trans }}</i>
            </h1>
        </div>
        <div class="card-body">
            <div class="card clickable" onclick="location = '{{ path('boutique') }}'">
                <img class="card-img-top" src="{{ asset('images/epicerie2.jpeg') }}">
            </div>
        </div>
    </div>
{% endblock %}
